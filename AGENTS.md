# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# 세션 간 연락은 `.session-mailbox/`

CCD 의 `send_message` 가 이 프로젝트에서 **양방향 모두 전달되지 않았다.**
다른 세션과 주고받을 것이 있으면 `.session-mailbox/README.md` 를 먼저 읽는다.
**지금 읽지 않은 메시지가 `.session-mailbox/to-app-session.md` 에 있다.**
