-- ────────────────────────────────────────────
--  003 — 홈 카드의 "나중에"
--
--  C 홈의 도전 제안 카드에는 "나중에" 버튼이 있다.
--  이걸 avoiding 으로 바꾸면 영영 다시 안 나온다 — 너무 세다.
--  "나중에" 는 거절이 아니라 미루기다.
--
--  그래서 미룬 기한만 따로 둔다. 기한이 지나면 다시 제안된다.
-- ────────────────────────────────────────────

BEGIN;

ALTER TABLE my_table_items
    ADD COLUMN IF NOT EXISTS snoozed_until DATE;

COMMIT;
