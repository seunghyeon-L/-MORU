# MORU Frontend

## 1. 서비스 개요

MORU는 음식으로 인한 불편함이나 음식 제한을 경험하는 사용자가
식사, 증상, 컨디션을 기록하고 반복되는 패턴을 이해하며
불필요한 음식 제한을 줄이고 음식 선택 범위를 넓혀가는 모바일 웰니스 앱이다.

의료 진단·치료 서비스가 아니며, AI는 진단이나 결정을 내리지 않고
사용자의 기록을 바탕으로 참고 정보와 선택지를 제공한다.

핵심 가치는 "무엇을 먹으면 안 되는가?"가 아니라
"어떤 상황에서 어떤 음식을 어느 정도까지 편하게 먹을 수 있는가?"를
이해하도록 돕는 것이다.

## 2. 핵심 기능

- 온보딩 / 안전 확인
- 알레르기 및 음식 회피 정보
- 음식 기록
- 음식 사진 / 메뉴 입력
- 음식 섭취 전 참고 정보 및 대안 제안
- 증상 기록
- 개인화된 패턴 분석
- 음식 재도입
- 나의 식탁

## 3. 핵심 원칙

### 의료 관련
- 질병을 진단하거나 치료하지 않는다.
- 안전 스크리닝은 AI가 판단하지 않는다.
- 위험 신호가 있으면 의료기관 안내 화면으로 이동한다.
- 음식이나 증상을 원인으로 단정하지 않는다.

### AI / 분석
- AI는 의사결정 지원자 역할만 한다.
- 1회 기록으로 원인을 판단하지 않는다.
- 음식, 수면, 스트레스, 컨디션, 음주, 식사량 등 상황을 함께 고려한다.
- 데이터가 부족하면 확정적인 판단을 하지 않는다.
- "먹으면 안 됩니다" 같은 금지·명령형 표현을 사용하지 않는다.
- 중립적이고 제안적인 표현을 사용한다.

### 재도입
음식 재도입은 MORU의 핵심 기능이다.

기본 흐름:
다시 먹어볼 음식 발견
→ 컨디션 확인
→ 재도입 제안
→ 준비
→ 소량 시도
→ 증상 및 상황 기록
→ 결과 확인
→ 개인 데이터 업데이트

재도입 결과를 "허용 / 금지"로 단정하지 않는다.

## 4. UX 원칙

- 최소 입력
- 선택형 UI 우선
- 한 화면에 너무 많은 질문을 넣지 않는다.
- 증상이 없으면 간단하게 기록한다.
- 증상이 발생했을 때만 상세 입력을 제공한다.
- 기록을 강요하지 않는다.
- 불안을 높이는 표현을 피한다.
- 금지보다 선택지를 제공한다.

## 5. 기술

- React Native
- Expo
- TypeScript
- Expo Router
- Figma 디자인 기준 구현
- 현재 프로젝트 구조 유지
- 백엔드 연결 전에는 mock / local state 사용
- 사용자 선택값은 필요한 범위에서 로컬 상태로 관리

## 6. 개발 규칙

- 요청한 작업에 직접 필요한 파일만 확인하고 수정한다.
- 프로젝트 전체를 불필요하게 탐색하거나 재분석하지 않는다.
- 기존 컴포넌트 / 타입 / 훅 / 유틸리티를 먼저 재사용한다.
- 정상적으로 작동하는 기존 코드는 리팩터링하지 않는다.
- 중복 UI / 로직을 만들지 않는다.
- 필요한 경우에만 새 컴포넌트나 라이브러리를 추가한다.
- 요청하지 않은 기능 / 디자인 / 구조를 추가하지 않는다.
- 백엔드가 없을 때 실제 API나 복잡한 데이터 계층을 만들지 않는다.
- mock 데이터는 최소한으로 작성한다.
- 오류는 관련 파일부터 확인하고 필요한 범위까지만 수정한다.
- 작업이 명확하면 바로 구현한다.
- 작업 완료 후 변경한 파일과 핵심 변경사항만 짧게 요약한다.
- 전체 코드나 변경하지 않은 파일을 출력하지 않는다.